## Goal and outcome
- Goal: Update all CLI-facing prompt/help surfaces to match the new `acs` invocation model, clarify nested-directory root discovery, and provide explicit root-not-found guidance.
- Outcome: CLI help text, runtime/planner prompt usage text, README command docs, and architecture notes now consistently describe `acs` usage, nested directory support, npm fallback, and root-not-found remediation.

## Key code changes
- Updated CLI help output to include a dedicated root-discovery section:
  - `server/src/cli/index.ts`
  - Added guidance that commands can run from repo root or nested workspace directories.
  - Added fallback note: `npm run cli -w server -- <command>`.
  - Added explicit root-not-found message/remediation text.

- Updated generated CLI usage context in prompt surfaces:
  - `server/src/services/promptBuilder.ts`
  - `server/src/application/cliServices.ts`
  - `server/src/routes/plans.ts`
  - Replaced `/server`-only wording with `acs`-first nested-directory guidance.
  - Included explicit root-not-found remediation line.

- Updated command documentation:
  - `README.md`
  - CLI section now states `acs` works from repository root or nested directories in the workspace.
  - Added explicit example of root-not-found output and how to fix by changing directory.

- Updated architecture doc expectations:
  - `docs/cli-command-architecture.md`
  - Added explicit requirement for nested-directory root discovery and root-not-found help/error copy.

- Updated tests to enforce help consistency:
  - `server/src/integration.test.ts`
  - Extended help-output test to assert presence of new root-discovery guidance text.

## Tests/validation performed
- Ran CLI help directly from server workspace:
  - `npm run -s cli -- --help`
  - Result: pass; output includes new "Location and root discovery" section and remediation lines.

- Ran CLI integration suite subset:
  - `npm run -s test -- --test-name-pattern "CLI subcommands"`
  - Result: pass; all CLI subcommand tests succeeded, including updated help assertions and existing nested-cwd/root-failure wrapper tests.

## Remaining risks or follow-ups
- CLI usage text is duplicated across multiple files (`promptBuilder.ts`, `cliServices.ts`, `routes/plans.ts`, and help text). Future command/wording updates may drift unless centralized.
- README now documents the wrapper error text as a single-line message; if wrapper wording changes later, docs should be kept in sync.
- Optional follow-up: extract shared CLI usage/help text builder to reduce maintenance overhead and ensure exact consistency across runtime prompts and CLI help.
