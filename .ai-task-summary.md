# Task Summary

## Goal and outcome
- Goal: Inspect the repository for existing database access layers, task/plan/project models, and current CLI patterns; then define a command architecture for a new CLI tool that avoids duplicating business logic.
- Outcome: Completed. Added a new architecture document at `docs/cli-command-architecture.md` that inventories reusable modules and proposes a thin-CLI + shared-application-service architecture.

## Key code changes
- Added new documentation file:
  - `docs/cli-command-architecture.md`
- Documented existing reusable layers:
  - DB/bootstrap and persistence split modules (`server/src/db/*`)
  - Type/model definitions (`server/src/types.ts`)
  - Runtime/task/plan/project orchestration modules (`server/src/routes/*`, `server/src/services/*`)
  - Existing command execution patterns (`git`, `tmux`, adapter command building)
- Defined concrete CLI architecture and command tree:
  - Proposed `server/src/cli/*` transport layer
  - Proposed `server/src/application/*` shared business-logic layer
  - Reuse existing infrastructure modules for DB, runtime, git, adapters
- Identified route-coupled logic that should be extracted to shared services before CLI implementation.

## Tests/validation performed
- Repository-wide discovery and code inspection using `rg`, `ls`, `sed`, and `nl` across:
  - `server/src/db`, `server/src/routes`, `server/src/services`, `server/src/utils`, `docs`
- Verified key exports and call sites to ensure the proposed architecture maps to current code boundaries.
- Validated the new summary/architecture doc content by re-reading the created file.

## Remaining risks or follow-ups
- Risk: Some business logic remains embedded in route files (`tasks.ts`, `plans.ts`, `projects.ts`), so building a CLI immediately would duplicate logic unless extracted.
- Follow-up: Extract route-private domain operations into `server/src/application/{project,task,plan,settings}Service.ts` and have both API routes and CLI call those services.
- Follow-up: Add CLI integration tests for parity with existing API behavior (task create/start/input/merge and plan extract/approve/regenerate flows).
- Follow-up: Standardize domain error types for stable CLI exit-code mapping.
