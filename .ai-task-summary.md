# Task Summary

## Goal and Outcome
- Goal: Create a full runtime template for decomposing a Plan node into `sub_plan` and `execution_task` items while preserving DAG and approval/materialization constraints.
- Outcome: Added `prompts/plan-to-subplans-and-tasks.md` with deterministic item-type rules, parser-compatible YAML requirements, recursion-depth limits, dependency topology safeguards, and idempotency decomposition fingerprint requirements.

## Key Code Changes
- Added new prompt artifact:
  - `prompts/plan-to-subplans-and-tasks.md`
- Template content aligns with current implementation constraints in:
  - `server/src/services/planParser.ts`
  - `server/src/application/cliServices.ts`
- Included explicit requirements for:
  - `item_type` decision rules (`execution_task` vs `sub_plan`)
  - YAML fields compatible with parser (`id`, `title`, `prompt`, `item_type`, `depends_on`)
  - Top-level defaults (`auto_start`, `auto_merge_on_complete`, `auto_merge_item_keys`)
  - Automation field compatibility by item type
  - Recursion depth guard (`max_sub_plan_recursion_depth: 6`)
  - Dependency/DAG validation and topology compatibility
  - Structured payload extension with per-item `decomposition_fingerprint`

## Tests/Validation Performed
- Ran CLI help from `/server`:
  - `npm run cli -- --help`
- Cross-checked template constraints against:
  - `server/src/services/planParser.ts` parsing/validation behavior
  - `server/src/application/cliServices.ts` approval/materialization, topology, and recursion checks
- Verified file creation and content:
  - `prompts/plan-to-subplans-and-tasks.md`

## Remaining Risks or Follow-ups
- Risk: This is documentation/template-level enforcement; runtime schema validation for new `plan_decomposition` extension is not yet wired in code.
- Follow-up: Add explicit validator hooks at extract/approve boundaries for template extension fields.
- Follow-up: Add integration tests that fail on missing/invalid `decomposition_fingerprint` and topology scope metadata.
