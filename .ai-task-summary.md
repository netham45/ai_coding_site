## Goal and outcome
- Goal: Execute the implementation prompt by adding staged hierarchical-orchestration rollout controls while preserving existing task/plan compatibility, and ensure coverage for cross-tier DAG behavior, unblocking/delta-loop correctness, merge/conflict behavior, and UI smoke paths.
- Outcome: Added server-side orchestration feature flags and compatibility mode, expanded integration/unit coverage, introduced frontend smoke-test infrastructure with start-from-tier/automation-toggle tests, and documented rollout, rollback, and observability guidance.

## Key code changes
- Added feature-flag configuration for staged rollout control:
  - `server/src/config/featureFlags.ts`
  - Flags: `ORCHESTRATION_COMPATIBILITY_MODE`, `ORCHESTRATION_WORKERS_ENABLED`, `ORCHESTRATION_HIERARCHY_API_ENABLED`, `ORCHESTRATION_ACTIONS_API_ENABLED`.
- Gated orchestration workers behind feature flags:
  - `server/src/index.ts`
  - `startOrchestrationJobQueueWorker`, `startHierarchicalOrchestrationJobs`, and `startPlanOrchestrationWorker` now run only when orchestration workers are enabled.
- Gated orchestration hierarchy/action APIs with explicit disabled responses:
  - `server/src/routes/tasks.ts`
  - Added `FEATURE_DISABLED` responses for hierarchy endpoints (`/api/projects/:projectId/hierarchy`, `/api/projects/:projectId/dependency-graph`, `/api/nodes/:nodeId`) and orchestration action endpoints (`/api/nodes/:nodeId/*`).
- Expanded server integration coverage for rollout compatibility:
  - `server/src/integration.test.ts`
  - Added tests proving:
    - compatibility mode disables orchestration endpoints while core task APIs remain functional,
    - hierarchy/action flags can be disabled independently.
- Added dedicated orchestration service unit tests:
  - `server/src/services/orchestration/dependencyGraph.test.ts`
  - `server/src/services/orchestration/stateMachine.test.ts`
- Updated server test script to include orchestration unit tests:
  - `server/package.json`
- Added web smoke-test infrastructure and smoke tests:
  - `web/package.json` (`vitest`, `jsdom`, Testing Library deps, `npm run test` script)
  - `web/vite.config.ts` (`test` config)
  - `web/src/test/setup.ts`
  - `web/src/components/OrchestrationControls.tsx`
  - `web/src/components/OrchestrationControls.test.tsx`
- Added staged rollout and rollback documentation:
  - `docs/rollout/hierarchical-orchestration-rollout.md`

## Tests/validation performed
- Required CLI surface check:
  - `cd server && npm run cli -- --help` (passed).
- Server tests:
  - `cd server && npm test` (passed).
  - Result: 54 tests passed, 0 failures (integration + new orchestration unit tests).
- Web tests:
  - `cd web && npm run test` (passed).
  - Result: 2 smoke tests passed, 0 failures.
- Build checks:
  - `cd server && npm run build` (passed).
  - `cd web && npm run build` (passed).
- Dependency/install step:
  - `npm install` run at workspace root to add web test dependencies and refresh lockfile.

## Remaining risks or follow-ups
- Frontend smoke tests currently target a focused orchestration controls component; they do not yet assert full-page wiring in `ProjectDetailPage`/`TaskDetailPage` against live API calls.
- Feature flags currently return `404 FEATURE_DISABLED`; if product/API contracts require alternate status codes or structured error envelopes, align that behavior before wider rollout.
- Rollout observability metrics are documented, but no new automated dashboards/alerts were created in this task.
- Web build still reports Vite large-chunk warning (>500 kB); code splitting remains a separate optimization.
- Goal: fix rerun failures caused by `NOT NULL constraint failed: tasks.base_commit_sha_at_create`.
- Outcome: rerunning a task now preserves the schema constraint by setting `base_commit_sha_at_create` to a valid source commit SHA instead of `NULL`.

## Key code changes
- Updated rerun logic in `server/src/routes/tasks.ts` (`POST /tasks/:taskId/rerun`):
  - Determine rerun source path:
    - default: `project.base_path`
    - if plan child task: parent plan task workspace path
  - Compute `baseCommitSha` using `getHeadCommitSha(sourcePath)`.
  - Update task reset SQL to write `base_commit_sha_at_create = ?` with `baseCommitSha` (previously set to `NULL`).

- Added regression coverage in `server/src/integration.test.ts`:
  - Added helper `gitHead(cwd)`.
  - New test: `task rerun resets task without violating base commit NOT NULL constraint`.
  - Test creates a repo with two commits, inserts a failed task with the first SHA, reruns the task, and verifies:
    - API returns success
    - `baseCommitShaAtCreate` equals latest repo HEAD
    - DB row status is `queued` and `base_commit_sha_at_create` is non-null/latest SHA.

## Tests/validation performed
- Ran full server integration tests:
  - `cd server && npm test -- --runInBand`
- Result: all tests passed (`26` tests, `0` failures).

## Remaining risks or follow-ups
- Current regression test covers execution-mode tasks rerun from project base repo; consider adding a dedicated test for plan-child tasks rerun from parent plan workspace to validate source-path selection in that branch.
- If repository state is unavailable/corrupt at rerun time, endpoint behavior still returns conflict/error as designed; no additional retry strategy was introduced.
