## Goal and outcome
- Goal: prevent divergence by making legacy create routes (`/projects/:projectId/tasks` and `/projects/:projectId/plans`) thin wrappers around the unified node creation behavior.
- Outcome: implemented shared creation logic in `tasks.ts` and routed all three create endpoints (`/nodes`, `/tasks`, `/plans`) through it while preserving legacy response shapes and tier defaults.

## Key code changes
- `server/src/routes/tasks.ts`
  - Added exported shared helper `createProjectNode(...)` with a typed input contract covering unified and legacy creation use cases.
  - Moved workspace initialization, dependency resolution/validation, metadata construction, insert/transition writes, event recording, and queue kick into shared logic.
  - Kept endpoint contracts:
    - `POST /projects/:projectId/nodes` returns `{ node }` and uses `source: "unified"`.
    - `POST /projects/:projectId/tasks` returns `{ task }` and uses `source: "legacyTask"`.
  - Preserved tier defaults:
    - legacy tasks map to `nodeTier: "task"`.
    - unified endpoint behavior for `epoch|phase|plan|task` unchanged.

- `server/src/routes/plans.ts`
  - Updated `POST /projects/:projectId/plans` to be a thin wrapper:
    - still applies planner prompt formatting (`buildPlanTaskPrompt(...)`).
    - delegates creation to `createProjectNode(...)` with `source: "legacyPlan"`.
    - still returns `{ plan }`.
  - Preserved tier default for plans (`nodeTier: "plan"`).

- `server/src/integration.test.ts`
  - Expanded integration coverage by updating the unified-node creation test to cover both legacy and unified paths in one flow.
  - New assertions verify:
    - legacy `/plans` create succeeds with `mode: "plan"` and metadata `tier: "plan"`.
    - legacy `/tasks` create succeeds with `mode: "execution"`, metadata `tier: "task"`, and dependency persistence.
    - unified `/nodes` still creates `epoch/phase/plan/task` with expected modes, parent linkage, and dependencies.

## Tests/validation performed
- Ran CLI help from `/server`:
  - `npm run cli -- --help`
- Built server:
  - `npm run build`
- Ran targeted test for this change (with required env override in this workspace):
  - `AI_CODING_ALLOW_NONISOLATED_TEST_PATHS=1 npm test -- --test-name-pattern "legacy and unified create endpoints produce expected tiers, modes, and dependencies"`
- Result: pass (`59 passed, 0 failed` in the test run output).

## Remaining risks or follow-ups
- `plans.ts` now imports creation logic from `tasks.ts`; this is intentional for shared behavior, but route-level coupling is increased. A follow-up extraction into a dedicated shared service module could reduce coupling.
- Error response text parity is preserved for known legacy parent-plan validation cases, but any undocumented reliance on exact wording for other failure paths should be verified by consumers.
- Only targeted integration coverage was added/updated for this refactor path; if desired, add explicit event-payload assertions for legacy wrappers to lock payload compatibility over time.
- Goal: Extend frontend API typing and client request helpers so orchestration endpoints can be consumed with explicit TypeScript contracts.
- Outcome: Added typed orchestration models and typed client helpers for unified node creation, hierarchy retrieval, dependency graph retrieval, node detail retrieval, and node action mutations. Web build passes after changes.

## Key code changes
- Updated `web/src/api/types.ts` with explicit orchestration types:
  - Added tier union: `NodeTier = "epoch" | "phase" | "plan" | "task" | "exec"`.
  - Added create-node models: `CreateNodePayload`, `CreateNodeResponse`, plus dependency ref type.
  - Added hierarchy models: `ProjectHierarchy`, `HierarchyNode`, `HierarchyNodeRow`, `GetHierarchyResponse`.
  - Added dependency graph models: `ProjectDependencyGraph`, `DependencyGraphNode`, `DependencyGraphEdge`, `GetDependencyGraphResponse`.
  - Added node detail/action models: `OrchestrationNodeDetail`, `StartNodePayload/Result`, `SetNodeAutoModePayload`, `SetNodeAutoMergePayload`, `ForceReReviewPayload`, `ApproveBudgetOverridePayload`, `NodeMutationResult`, `AsyncNodeActionResult`.
- Updated `web/src/api/client.ts` with typed helpers:
  - `createNode(projectId, payload)`
  - `getHierarchy(projectId)`
  - `getDependencyGraph(projectId)`
  - `getNode(nodeId)`
  - `startNode(nodeId, payload?)`
  - `setNodeAutoMode(nodeId, payload)`
  - `setNodeAutoMerge(nodeId, payload)`
  - `forceNodeReReview(nodeId, payload?)`
  - `approveNodeBudgetOverride(nodeId, payload?)`

## Tests/validation performed
- Ran web build/typecheck:
  - `npm run build -w web`
- Result:
  - TypeScript compile succeeded.
  - Vite production build succeeded.

## Remaining risks or follow-ups
- `OrchestrationNodeDetail` currently keeps some server payload sections (`dependencyDiagnostics`, `waiting`, `automation`, `orchestration`) as `unknown`; these can be tightened with richer explicit types in a follow-up.
- Existing page components still use direct `api(...)` calls for many non-orchestration endpoints; a broader client-helper migration could further reduce inline request typing.
- If strict “no any-based calls” is required across broader UI pages, additional cleanup of `catch (error: any)` patterns in page files is still pending.
