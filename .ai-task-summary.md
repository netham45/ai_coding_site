## Goal and outcome
- Goal: Let users declare orchestration dependencies across tiers from the UI (not only execution-task IDs), including tier and optional reason, and surface unresolved/blocked upstream references clearly.
- Outcome: Added a reusable dependency picker, integrated it into node creation and plan approval edit flows, and wired hierarchy/dependency-graph data into candidate generation so users can submit `dependencyNodeRefs` across tiers with dedupe/self-dependency prevention in UI.

## Key code changes
- Added `web/src/components/NodeDependencyPicker.tsx`:
  - Reusable dependency row editor with candidate select, tier select, and optional reason input.
  - Candidate badges show status, blocked state, and unresolved upstream reference counts.
  - Enforces UI-level normalization: de-duplicates by `id+tier`, trims reason, and prevents self-dependency when `selfNodeId` is provided.
- Updated `web/src/components/NodeCreateForm.tsx`:
  - Replaced previous ID-only dependency UI with `NodeDependencyPicker`.
  - Submits `dependencyNodeRefs` including `id`, `tier`, and optional `reason`.
- Updated `web/src/pages/ProjectDetailPage.tsx`:
  - Loads both hierarchy and dependency graph (`getHierarchy`, `getDependencyGraph`).
  - Builds richer dependency candidates from hierarchy rows plus dependency-graph nodes.
  - Computes unresolved counts from graph edges and waiting state for clearer upstream context.
- Updated `web/src/pages/TaskDetailPage.tsx`:
  - Loads hierarchy/dependency-graph project context for candidate sourcing.
  - Extends plan item drafts with `dependencyNodeRefs` and renders `NodeDependencyPicker` per proposed plan item.
  - Includes dependency refs in plan approval `taskEdits` payload.
  - Enhances dependency info panel to explicitly display unresolved upstream refs (tier/id/reason/status) when present.

## Tests/validation performed
- Ran frontend build and typecheck from `web/`:
  - `npm run build`
- Result:
  - TypeScript project build succeeded.
  - Vite production build succeeded.
  - Existing chunk-size warning remains non-blocking.

## Remaining risks or follow-ups
- Backend `POST /api/plans/:planId/approve` schema currently does not explicitly declare `taskEdits[].dependencyNodeRefs` in route validation; frontend now sends it, but server-side acceptance/persistence should be confirmed and, if needed, added.
- Candidate blocked state for nodes that exist only in dependency-graph fallback is inferred from unresolved edges when hierarchy row data is unavailable.
- `web/tsconfig.tsbuildinfo` changed during build; keep/ignore per repository policy.
- Goal: Expose node-level orchestration operations in the existing task/plan detail UX so users can operate orchestration flows directly in the web UI.
- Outcome: Added node orchestration detail loading and a dedicated UI panel for lineage, blocking diagnostics, unresolved dependency reasons, and orchestration actions. Actions now execute against `/api/nodes/:nodeId/*` endpoints and refresh detail state after success.

## Key code changes
- Added [web/src/components/NodeActionsPanel.tsx](web/src/components/NodeActionsPanel.tsx):
  - Renders node tier + mode, parent and children overview.
  - Parses and displays waiting/blocking diagnostics and unresolved dependency reason details from node APIs.
  - Adds orchestration controls for:
    - Start node (`/api/nodes/:nodeId/start`)
    - Auto-mode toggle (`/api/nodes/:nodeId/auto-mode`)
    - Auto-merge toggle (`/api/nodes/:nodeId/auto-merge`)
    - Force re-review (`/api/nodes/:nodeId/force-re-review`)
    - Budget override approval (`/api/nodes/:nodeId/approve-budget-override`)
- Updated [web/src/pages/TaskDetailPage.tsx](web/src/pages/TaskDetailPage.tsx):
  - Imports typed orchestration client helpers and `NodeActionsPanel`.
  - Adds node detail state (`nodeDetail`, loading/error state, node action loading state).
  - Adds `loadNodeDetails` using `getNode(entityId)`.
  - Loads node details on entity change and alongside existing polling.
  - Adds shared node action executor that calls endpoint helpers, shows toast feedback, and refreshes both task detail + node detail state.
  - Integrates `NodeActionsPanel` in Task Info view.

## Tests/validation performed
- Ran `npm run build -w web`:
  - TypeScript project build succeeded.
  - Vite production build succeeded.
- Ran `npm run cli -- --help` from `server/` per prompt constraints:
  - CLI command and help output succeeded.

## Remaining risks or follow-ups
- Node detail payload sections (`waiting`, `dependencyDiagnostics`, `orchestration`, `automation`) are still typed as `unknown` at API type level and are parsed defensively in UI; stronger explicit frontend types would reduce runtime parsing logic.
- The page now polls both task and node endpoints every 3 seconds; if endpoint volume becomes a concern, polling cadence or conditional refresh triggers may need tuning.
- `web/tsconfig.tsbuildinfo` is updated as a build artifact; repository policy should decide whether to track or ignore it.
