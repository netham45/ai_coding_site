## Goal and outcome
- Goal: Complete and version all required runtime prompt artifacts for orchestration flows, then strengthen them to require research before decomposition/synthesis/verification decisions.
- Outcome: All 11 required prompt files are present and updated with full runtime contract sections and structured payloads. Missing artifacts were added, and research-first behavior is now explicitly required before sub-stage planning or readiness decisions.

## Key code changes
- Reworked prompt contracts for required files:
  - `prompts/epoch-to-phases.md`
  - `prompts/phase-to-plans.md`
  - `prompts/plan-to-subplans-and-tasks.md`
  - `prompts/task-to-exec-tasks.md` (new)
  - `prompts/exec-task-runner.md`
  - `prompts/readiness-evaluation.md`
  - `prompts/re-review-unblock.md` (new)
  - `prompts/synthesis.md`
  - `prompts/verification.md`
  - `prompts/delta-planning.md`
  - `prompts/intent-preserving-conflict-resolution.md`
- Added/standardized sections across all required prompts:
  - goal, non-goals, definition of done, dependencies, artifacts, risks, idempotency, bounded iteration, auto-merge guidance, structured output contract.
- Added research-first requirements in decomposition/evaluation flows:
  - mandatory CLI tree inspection (plans/tasks/info/details queries),
  - mandatory code/documentation review,
  - required `research_evidence` payload block (`cli_queries`, `repo_reads`, `tree_coverage`).
- Restored `.ai-task-summary.md` in workspace root for runtime summary artifact.

## Tests/validation performed
- Ran required CLI help command from `/server`:
  - `npm run cli -- --help`
- Verified all 11 required prompt files exist under `prompts/`.
- Validated required contract section headings are present in each target prompt file.
- Validated research-first language and `research_evidence` blocks exist in updated decomposition/synthesis/verification prompts.
- Checked git state to ensure all workspace changes are staged/committable.

## Remaining risks or follow-ups
- These are prompt/template changes; no full end-to-end runtime orchestration execution was run in this task.
- If strict parser validation exists for prompt payload fields, added extension blocks (for example `research_evidence`) may require schema alignment where consumed.
- Follow-up: run one orchestration dry run across epoch -> phase -> plan -> task -> exec and synthesis/verification loops to confirm practical behavior and schema compatibility.
