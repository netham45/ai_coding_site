# Task Summary

## Goal and Outcome
- Goal: Create the full runtime prompt template that decomposes an Epoch node into Phase children, aligned with the established prompt IO contract.
- Outcome: Added a complete `epoch-to-phases` runtime template with required inputs, decomposition rules, bounded iteration/escalation behavior, and a structured output schema containing all required contract fields plus cross-tier dependency reasons.

## Key Code Changes
- Added new prompt artifact:
  - `prompts/epoch-to-phases.md`
  - Includes:
    - Tier responsibilities: strategic scope, milestone slicing, cross-phase dependency reasoning, risk front-loading.
    - Required inputs: epoch metadata/spec, project constraints, existing DAG state, repo context, unresolved blockers.
    - Required two-part output: natural-language phase strategy + structured YAML payload.
    - Structured payload fields required by contract: `goals`, `non_goals`, `definition_of_done`, `deps`, `artifacts`, `risks`, `idempotency`, `bounded_iteration`, `auto_merge_guidance`.
    - Additional decomposition-specific fields: `phase_children`, `cross_tier_dependency_reasons`, `decomposition_fingerprint`, `assumptions`, `escalations`.
    - Explicit bounded-pass stop/escalate behavior when decomposition limits are reached.
- Updated prompt index:
  - `prompts/README.md`
  - Added `epoch-to-phases.md` to the runtime prompt artifact list.

## Tests/Validation Performed
- Ran CLI help from `/server` to validate command surface as required:
  - `npm run cli -- --help`
- Verified new artifact content and required schema coverage:
  - Confirmed presence of all contract-required output fields.
  - Confirmed explicit cross-tier dependency reason section.
  - Confirmed explicit bounded iteration and escalation instructions.
- Verified file wiring/discoverability:
  - Confirmed `prompts/README.md` includes `epoch-to-phases.md`.

## Remaining Risks or Follow-ups
- Risk: The new template defines `phase_children` and decomposition-specific fields beyond the base prompt-contract schema; downstream validators/parsers may need to explicitly tolerate or validate these additions.
- Risk: No automated tests currently assert schema compliance for this new template artifact.
- Follow-up: Add/extend prompt validation tests to enforce required fields and ensure decomposition-specific fields are either accepted or formally added to the contract docs.
- Follow-up: Optionally add a concrete valid/invalid `epoch-to-phases` payload example in architecture docs to speed adoption and reduce ambiguity.
