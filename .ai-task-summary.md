# Task Summary

## Goal and outcome
- Goal: Update user-facing and developer docs for the new plan automation model, YAML contract, CLI options, and state machine, including migration notes, operational cautions, and a staged rollout checklist.
- Outcome: Added a dedicated runbook-style document and updated `README.md` with a plan automation section, command examples, and links to deeper guidance aligned to current implementation behavior.

## Key code changes
- Added `docs/plan-automation-model.md` covering:
  - Automation trigger model (`mode=plan`, `auto_start=1`, `status=waiting_input`, plan file present)
  - Output-hash idempotency and retry semantics after changed YAML
  - YAML schema/validation rules and backward compatibility expectations
  - CLI flags for `plans create`/`plans approve` automation controls
  - Practical task/plan state machine and automation event references
  - Migration notes, operational cautions, and staged rollout checklist
- Updated `README.md`:
  - Added `Plan automation` entry to the table of contents
  - Added plan automation capability note in features
  - Added CLI examples for plan automation flags
  - Added a new `## Plan automation` section summarizing behavior and linking to `docs/plan-automation-model.md`

## Tests/validation performed
- From `/server`:
  - `npm run cli -- --help` (passed)
- Verified documentation command/flag examples against current CLI help output.

## Remaining risks or follow-ups
- No automated tests were run for docs-only edits (`npm test`/`npm run build` not rerun in this task).
- Multi-process orchestration coalescing/fairness remains an operational consideration; current worker limits and lock semantics should be monitored during rollout.
- Future follow-up: add links from UI/help surfaces to `docs/plan-automation-model.md` for operator discoverability.
