# Task Summary

## Goal and outcome
Goal: Implement the app-vs-project SQLite ownership split from `docs/adr-sqlite-persistence-split.md`.
Outcome: Completed. Global entities remain in `data/app.sqlite`, and project-local entities now live in `<project.base_path>/.ai-coding/project.sqlite`.

## Key code changes
- Split schema ownership into app DB and project DB in `server/src/db/migrations.ts`.
- Refactored DB layer in `server/src/db/index.ts`:
  - `appDb` for global tables.
  - Per-project DB open/init/cache/validation helpers.
  - `project_metadata` + `project_config` handling.
  - Explicit invariants comments: single owner per table, no cross-file FKs, ID-only cross-DB links.
- Added `server/src/db/ownership.ts` for project/task DB context resolution.
- Rewired routes/services/ws to enforce ownership boundaries:
  - App DB for users/settings/projects/membership.
  - Project DB for tasks/plans/sessions/events/IDE/merge and project config fields.
- Updated `server/src/types.ts` and `README.md` for the split model.

## Tests/validation performed
- `npm run build -w server` passed.
- `npm run build` (server + web) passed.

## Remaining risks or follow-ups
- Historical data migration from monolithic app DB runtime tables to per-project DBs is not included.
- Some cross-project task resolution paths are scan-based and may need optimization at scale.
- Operational repair/recovery tooling for unavailable/corrupt project DBs can be expanded.
