# Task Summary

## Goal and outcome
- Goal: Add a coordinator prompt template for `evaluate_readiness(node)` that performs deterministic readiness/state-transition evaluation.
- Outcome: Created `prompts/readiness-evaluation.md` with required inputs, deterministic lifecycle rules, explicit transition reason codes, and structured output requirements; updated prompt artifact index in `prompts/README.md`.

## Key code changes
- Added new prompt artifact:
  - `prompts/readiness-evaluation.md`
  - Includes:
    - Supported lifecycle states: `draft | ready | blocked | running | complete | failed | canceled`
    - Required inputs: node metadata, dependency graph status, child status summary, merge prerequisites, verification prerequisites
    - Deterministic evaluation rules including cross-tier dependency handling and sticky terminal states
    - Required reason-code taxonomy for transitions (`readiness.reason_codes`)
    - Required structured readiness payload fields:
      - `readiness.current_state`
      - `readiness.recommended_state`
      - `readiness.allowed_transition`
      - `readiness.reason_codes`
      - `readiness.blockers`
      - `readiness.follow_up_jobs`
      - `readiness.idempotency_key`
- Updated prompt index:
  - `prompts/README.md`
  - Added `readiness-evaluation.md` to the file list.

## Tests/validation performed
- Ran CLI help from `/server`:
  - `npm run cli -- --help`
  - Confirmed command surface is available and aligned with expected usage.
- Verified artifact content and diffs:
  - Checked `prompts/readiness-evaluation.md` content for required inputs/outputs and reason codes.
  - Confirmed `prompts/README.md` includes the new template.

## Remaining risks or follow-ups
- Risk: Template contract is documented but not yet enforced by runtime schema validation before persistence/materialization.
- Follow-up: Add runtime validator for readiness output (including allowed states and required reason codes).
- Follow-up: Add integration tests for readiness evaluator to fail on missing/invalid `readiness.*` fields.
- Follow-up: Optionally add valid/invalid readiness payload examples to improve coordinator consistency.
