# Task Summary

<<<<<<< HEAD
## Goal and Outcome
- Goal: Resolve auto-merge conflict between task-branch updates while preserving both prompt-template additions.
- Outcome: Merged conflict paths successfully, retaining both `synthesis(parent)` and `re_review(node)` coordinator artifacts and producing a clean workspace state for auto-merge.

## Key Code Changes
- Resolved merge conflicts in:
  - `.ai-task-summary.md`
  - `prompts/README.md`
- Kept both prompt templates in the merged branch:
  - `prompts/synthesis.md` - requirement-evidence coverage synthesis contract, uncovered requirements, and candidate gap hashes.
  - `prompts/re-review-unblock.md` - unblock vs still-blocked vs delta-work re-review decision contract with debounce/anti-thrash guidance.
- Updated prompt index (`prompts/README.md`) so both templates are listed.

## Tests/Validation Performed
- Verified conflict markers were removed from both conflicted files.
- Verified prompt index contains both entries (`synthesis.md`, `re-review-unblock.md`).
- Verified repository status after merge resolution and commit is clean.

## Remaining Risks or Follow-ups
- Risk: Prompt schemas are documented artifacts; runtime schema enforcement for these payload extensions may still need explicit validators.
- Follow-up: Add parser/orchestrator validation and integration tests for synthesis coverage-matrix rules and re-review single-action/debounce behavior.
=======
## Goal and outcome
<<<<<<< HEAD

### Phase-to-plans prompt artifact
- Goal: Create the runtime prompt template that decomposes a Phase node into Plan children, with strict contract-aligned inputs/outputs and support for both initial decomposition and refinement.
- Outcome: Added `prompts/phase-to-plans.md` defining required inputs, decomposition rules, structured output schema, and validation rules; updated prompt index docs.

### Re-review unblock prompt artifact
- Goal: Add a coordinator prompt template for `re_review(node)` that determines whether a node is truly unblocked, still blocked, or requires new delta child work.
- Outcome: Added `prompts/re-review-unblock.md` with single-action decision rules, anti-thrash/debounce guidance, and bounded retry/escalation semantics aligned to the prompt contract.

## Key code changes
- Added `prompts/phase-to-plans.md`:
  - Inputs include phase spec, epoch goals, dependency statuses, prior synthesis/verification outputs, repository context, and `mode` (`initial`/`refine`).
  - Requires dual output: rationale + structured payload matching `docs/architecture/prompt-contract.md`.
  - Includes plan decomposition structure with sequencing, dependency reasons, deliverables/modules, risks, idempotency, iteration controls, auto-merge defaults, and refine `change_summary`.
- Added `prompts/re-review-unblock.md`:
  - Defines required re-review inputs for changed deps/children/events, synthesis/verification state, counters, and prior gap hashes.
  - Defines exactly-one-action decision output (`unblocked | still_blocked | spawn_delta_work`) and debounce rules.
  - Adds `re_review` structured payload extension with blocker/trigger evidence, gap hashes, retry guidance, and escalation.
- Updated `prompts/README.md`:
  - Added `phase-to-plans.md` and `re-review-unblock.md` to runtime prompt artifact index.
=======
- Goal: Deliver runtime prompt artifacts that strengthen orchestration quality at planning and re-review boundaries.
- Outcome: Added two prompt templates:
  - `prompts/plan-to-subplans-and-tasks.md` for deterministic Plan decomposition into `sub_plan` and `execution_task` DAG items.
  - `prompts/re-review-unblock.md` for `re_review(node)` decisions (`unblocked`, `still_blocked`, `spawn_delta_work`) with anti-thrash controls.

## Key code changes
- Added `prompts/plan-to-subplans-and-tasks.md`:
  - Explicit `item_type` decision rules with `execution_task` default on ambiguity.
  - Parser-compatible YAML requirements (`id`, `title`, `prompt`, `item_type`, `depends_on`).
  - Required defaults and automation constraints (`auto_start`, `auto_merge_on_complete`, `auto_merge_item_keys`, plus item-level rules).
  - DAG + topology safeguards and recursion depth guard (`max_sub_plan_recursion_depth: 6`).
  - Structured payload extension with per-item idempotency `decomposition_fingerprint`.
- Added `prompts/re-review-unblock.md`:
  - Single-action re-review output contract.
  - Dependency/child-completion and new-signal decision triggers.
  - Debounce/anti-thrash rules (stable blocker hash handling, duplicate gap suppression).
  - Bounded retry and escalation requirements.
  - Structured payload extension for evidence, gap hashes, retry guidance, and escalation.
- Updated `prompts/README.md`:
  - Added `re-review-unblock.md` to runtime template index.
>>>>>>> origin/task/925c8bf5-af1e-4a0b-aa9a-5ad190818b5b

## Tests/validation performed
- Ran CLI help from `/server`:
  - `npm run cli -- --help`
<<<<<<< HEAD
  - Confirmed expected command surface.
- Verified prompt artifact consistency against existing contract/template docs:
  - `docs/architecture/prompt-contract.md`
  - `prompts/shared-input-output.md`
  - Existing `prompts/*.md` templates.
- Validated file creation and index updates via workspace diff/status checks.

## Remaining risks or follow-ups
- Risk: Prompt contract conformance is documented in templates, but schema enforcement is not yet fully wired at runtime boundaries.
- Follow-up: Add validator support for phase decomposition payload checks and `re_review` action/payload constraints.
- Follow-up: Add integration tests for missing required fields, single-action enforcement, and budget-exhausted escalation behavior.
=======
- Cross-checked constraints against implementation behavior:
  - `server/src/services/planParser.ts`
  - `server/src/application/cliServices.ts`
- Reviewed prompt contract/template consistency:
  - `docs/architecture/prompt-contract.md`
  - `prompts/shared-input-output.md`
  - Existing prompt templates under `prompts/`
- Verified created/updated artifacts and merge resolution:
  - `prompts/plan-to-subplans-and-tasks.md`
  - `prompts/re-review-unblock.md`
  - `prompts/README.md`
  - `.ai-task-summary.md`

## Remaining risks or follow-ups
- Risk: New template-level payload extensions (`plan_decomposition`, `re_review`) are not yet enforced by runtime schema validators.
- Risk: Re-review debounce depends on upstream deterministic blocker/gap hashing.
- Follow-up: Add validator enforcement at extract/approve/orchestrator boundaries for both extensions.
- Follow-up: Add integration tests for:
  - missing/invalid decomposition fingerprints
  - topology/recursion guard failures
  - re-review single-action exclusivity and budget-exhausted escalation
  - duplicate gap-hash suppression behavior.
>>>>>>> origin/task/925c8bf5-af1e-4a0b-aa9a-5ad190818b5b
