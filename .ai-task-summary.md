# Task Summary

## Goal and outcome
Goal: Enforce the SQLite ownership split so app DB `projects` keeps only global metadata, while project-local prompt/config fields remain project-DB-owned, with compatibility handling for existing app DBs.
Outcome: Completed. The task branch now includes compatibility migration behavior in app DB bootstrap for legacy `projects` schemas, and conflicts from dependency sync were resolved.

## Key code changes
- Updated `server/src/db/index.ts`:
  - Added schema introspection helper for app DB table columns.
  - Added `migrateProjectsToGlobalMetadataOnly(appDb)` to detect legacy `projects` prompt/config columns and rebuild `projects` transactionally into global-metadata-only shape.
  - Recreated key project indexes after rebuild.
  - Kept split-DB app/project bootstrap logic intact.
- Resolved auto-merge conflicts in:
  - `server/src/db/index.ts`
  - `.ai-task-summary.md`
- Synced branch to latest dependency line before final conflict resolution.

## Tests/validation performed
- Ran `npm run build -w server`.
- Result: passed (`tsc -p tsconfig.json`).

## Remaining risks or follow-ups
- This compatibility path removes legacy app DB prompt/config columns but does not backfill those legacy values into per-project `project_config`; add explicit migration/backfill if required for historical data retention.
- Validate startup migration behavior against representative legacy `data/app.sqlite` files in a staging run.
